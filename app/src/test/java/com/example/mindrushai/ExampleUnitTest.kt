package com.example.mindrushai

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Basic sanity test — verifies the test infrastructure itself is working.
 */
class ExampleUnitTest {

    @Test
    fun `addition is correct`() {
        assertEquals(4, 2 + 2)
    }
}